enum Role {
  USER
  AGENT
  ADMIN
}

enum UserStatus {
  ACTIVE
  SUSPENDED
}

enum AuthProvider {
  CREDENTIAL
  GOOGLE
}

enum PackageStatus {
  PENDING
  APPROVED
  REJECTED
}

enum BookingStatus {
  PENDING
  CONFIRMED
  CANCELLED
  COMPLETED
}

enum PostStatus {
  DRAFT
  PUBLISHED
}
