# Step 13 — Seed Script

5 categories + 3 demo accounts (one per role) + 6 packages (with real Cloudinary-hosted image URLs, not placeholders) + ~15-20 bookings spread across different months/statuses + a handful of reviews + 3-4 blog posts (Step 11, published + one draft, real cover images) — so every dashboard chart (Step 12) and the landing Pages/Blog sections show real data on first run instead of rendering empty during a demo.
